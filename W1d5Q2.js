let prompt =require ('prompt-sync')();


//let area = Math.PI*r*r;
        // let r = parseInt(prompt("enter radius"));
        // while (r < 0) {
        //     r = prompt("enter radius")
        //     r = parseInt(r);
        // }
        // console.log("area = " + Math.PI * r * r);
/*
        function areaOfCircle(r,r){
        let radius;
        do {
            radius = +prompt("Enter radius of the circle");
            
        }
        while (radius <= 0);
        let area = Math.PI * Math.pow(radius, 2);
        console.log(area);
        */