//Write a swap function that takes two parameters of number type and swap the values of the
//passed parameters. 
function swap(x,y){
     t = x;
    x = y;
    y = t;
   }
   
   
   let x = 5;
   let y = 7;
   console.log(x,y) // 5, 7
  swap(x,y);
   console.log(x,y)